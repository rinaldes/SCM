$('#supplier').val('<%= @brand.supplier.name %>')
$('#m_class_0').val('<%= @brand.department.name %>')
$('#brand_discount1').val('<%= @brand.discount_percent1.to_f %>')
$('#brand_discount2').val('<%= @brand.discount_percent2.to_f %>')
$('#brand_discount3').val('<%= @brand.discount_percent3.to_f %>')
$('#brand_discount4').val('<%= @brand.discount_percent4.to_f %>')
$('#brand_price').val('<%= @brand.set_harga %>')
set_price()