$("#myModal").html("<%= j( render 'receiving_list')%>")
$("#myModal").modal()
paging()
$(function () {
        $('.pagination a').click(function () {
                $.get(this.href, null, null, 'script');
                return false;
        });
});
