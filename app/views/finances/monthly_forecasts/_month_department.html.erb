<%= form_tag edit_finances_forecast_monthly_forecast_path(@monthly_forecast.forecast_id, @monthly_forecast.id), :id => "monthly-forecasts-filter-form", class: "smart-form", :remote => false  do %>
    <section>
        <div class="row">
            <section class="col col-6">
                <div class="form-group">
                    <label class="col-md-2 control-label">Month</label>
                    <div class="col-md-10">
                        <%= select_tag "month", options_for_select(( @monthly_forecast.forecast.monthly_forecasts.pluck('month_number').uniq      ).map {|m| [Date::MONTHNAMES[m], m]}, (@monthly_forecast.month_number)) ,  {:class => "form-control", id: "select-month-forecast"} %>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-md-2 control-label">&nbsp;</label>
                    <div class="col-md-10">&nbsp;</div>
                </div>

            </section>
            <section class="col col-6 total-monthly-forecast">
                <%= render 'total_monthly_forecast' %>
            </section>
        </div>
    </section>
<% end %>