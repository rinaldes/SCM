<% if @areas  %>
  $('#branch_area_id').html("<option>Please Select</option>");
  $('#branch_area_id').append("<%= escape_javascript options_from_collection_for_select(@areas, :id, :name) %>");
<% end %>
